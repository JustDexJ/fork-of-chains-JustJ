import { SexPermission } from "../SexPermission";

export class SexPermission_Full extends SexPermission {
  constructor() {
    super(
      "full",
      [
        /* tags */
      ],
      [
        /* disallowed tags */
      ],
    );
  }
}
